// const initialValue = 0;

// const totalGlobalExpenses = (globalExpenses) => {
//   if (globalExpenses) {
//     const expenses = globalExpenses.reduce(
//       (total, expense) =>
//         total + expense.value * expense.exchangeRates[expense.currency].ask,
//       initialValue
//     );

//     return expenses;
//   }
//   return initialValue;
// };

// export default totalGlobalExpenses;
